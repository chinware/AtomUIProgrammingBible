# Separator 概述

### 简介

简洁的分隔线组件，用于对内容进行分组和区分。

![AtomUI Separator组件](./images/horizontal.PNG)

### 主要功能

* 支持水平和垂直两种方向的分隔线
* 支持带文字的分隔线，文字可居左、居中、居右
* 支持纯文本样式的分隔线
* 支持实线（Solid）、点线（Dotted）、虚线（Dashed）三种线条变体
* 支持自定义线条颜色、文字颜色和线条宽度
* 支持通过 `SizeType` 设置不同的间距大小
